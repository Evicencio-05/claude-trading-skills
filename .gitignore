# OS-generated files
.DS_Store
Thumbs.db

# Python cache & build artifacts
__pycache__/
.pytest_cache/
*.py[cod]
*.pyo
*.so
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Tool configurations
.idea/
.vscode/
.env
.envrc
.mcp.json

# Logs & temp
logs/
*.log
*.tmp

# Jupyter
.ipynb_checkpoints/

# Skill output files — canonical location is ~/trading-research/
# Do not commit generated reports to the repo.
reports/
reviews/

# Robinhood auth (never commit)
.tokens/
robinhood.pickle

# Per-symbol runtime state (e.g. parabolic-short SSR carryover files).
# Kept out of commits so production state never leaks into the repo.
state/
# Sync state (contains position data — keep local)
state/pending_ingest.json
state/synced_positions.json

# CI artifacts
htmlcov/
.coverage
# .secrets.baseline is tracked — needed by detect-secrets pre-commit hook
