# Archived prompts

One-shot task prompts archived during the prompt learning migration (2026-05-30).

Do not reuse these files — regenerate from `state/prompt_learnings.yaml` `task_families` via [prompt-engine.md](../prompt-engine.md).

Knowledge extracted to task families: `thesis_manager_dev`, `doc_sync`, `infra_systemd`.
